<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="PhcfMenuDAO">


<select id="selectMenuList" resultType="java.util.HashMap">
/* PhcfMenuDAO.selectMenuList 사용여부가 'Y'인 메뉴 목록 가져오기 */
		SELECT	MENU_NM
					, PROGRM_FILE_NM
					, MENU_NO
					, UPPER_MENU_NO
					, MENU_ORDR
					, MENU_DC
		FROM	COMTNMENUINFO
		WHERE USE_YN = 'Y'
</select>
	
	
</mapper>